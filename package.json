{
  "name": "quickly",
  "version": "1.0.0",
  "description": "Une bibliothèque TypeScript avec du Rust pour la performance",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist pkg",
    "build:rust": "wasm-pack build --target nodejs --out-dir pkg",
    "build:ts": "webpack --mode production",
    "build": "npm run clean && npm run build:rust && npm run build:ts && npm run copy:wasm",
    "copy:wasm": "cp pkg/*.wasm dist/",
    "dev": "webpack --mode development --watch",
    "compile:test": "webpack --config webpack.test.js",
    "test": "npm run build && npm run compile:test && node dist/test.js",
    "compile:bench": "webpack --config webpack.bench.js",
    "bench": "npm run build && npm run compile:bench && node dist/benchmark.js",
    "test:simple": "npm run build && webpack --config webpack.simple.js && cp pkg/*.wasm dist/ && node dist/simple-test.js",
    "test:all": "npm run test && npm run bench",
    "watch": "npm run build:rust && webpack --mode development --watch"
  },
  "keywords": [
    "performance",
    "rust",
    "wasm",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.15.30",
    "copy-webpack-plugin": "^12.0.2",
    "ts-loader": "^9.5.2",
    "typescript": "^5.8.3",
    "webpack": "^5.99.9",
    "webpack-cli": "^6.0.1"
  },
  "files": [
    "dist",
    "pkg"
  ]
}
