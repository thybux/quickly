{
  "name": "quickly",
  "version": "1.0.0",
  "description": "Une bibliothèque TypeScript avec du Rust pour la performance",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist pkg",
    "build:rust": "wasm-pack build --target nodejs --out-dir pkg",
    "build:ts": "webpack --mode production",
    "build": "npm run clean && npm run build:rust && npm run build:ts && npm run copy:wasm",
    "copy:wasm": "cp pkg/*.wasm dist/",
    "dev": "webpack --mode development --watch",
    "test:add": "npm run build && npx webpack --entry ./src/tests/array/math.test.ts --output-filename add-test.js --output-path ./dist --mode development --target node --resolve-extensions .ts,.js && cp pkg/*.wasm dist/ && node dist/add-test.js",
    "bench:add": "npm run build && npx webpack --entry ./src/tests/benchmarks/array-add.bench.ts --output-filename add-bench.js --output-path ./dist --mode development --target node --resolve-extensions .ts,.js && cp pkg/*.wasm dist/ && node dist/add-bench.js",
    "watch": "npm run build:rust && webpack --mode development --watch"
  },
  "keywords": [
    "performance",
    "rust",
    "wasm",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.15.30",
    "copy-webpack-plugin": "^12.0.2",
    "ts-loader": "^9.5.2",
    "typescript": "^5.8.3",
    "webpack": "^5.99.9",
    "webpack-cli": "^6.0.1"
  },
  "files": [
    "dist",
    "pkg"
  ]
}
